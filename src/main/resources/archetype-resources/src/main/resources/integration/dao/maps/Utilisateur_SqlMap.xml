#set( $symbol_pound = '#' )
#set( $symbol_dollar = '$' )
#set( $symbol_escape = '\' )
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="utilisateur">

    <resultMap type="${package}.business.bo.Utilisateur" id="mapUtilisateur">
       	<result column="ID_UTILISATEUR" property="utiId" />
   		<result column="UTI_LOGIN" property="login" />
    </resultMap>

	<sql id="selectUtilisateur_fragment">
		SELECT
		ID_UTILISATEUR,
		UTI_LOGIN
		FROM UTILISATEUR
	</sql>

	<select id="selectById" resultMap="mapUtilisateur">
		<include refid="selectUtilisateur_fragment" />
		WHERE ID_UTILISATEUR = ${symbol_pound}{utiId};
	</select>
	
	<select id="selectByLogin" resultMap="mapUtilisateur">
		<include refid="selectUtilisateur_fragment" />
		WHERE UTI_LOGIN like ${symbol_pound}{login};
	</select>
	
	<select id="selectByUserPassword" resultMap="mapUtilisateur">
		<include refid="selectUtilisateur_fragment" />
		WHERE UTI_LOGIN like ${symbol_pound}{login}
		AND UTI_PASSWORD like ${symbol_pound}{password};
	</select>

</mapper>